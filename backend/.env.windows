# ChatCRM Backend - Windows Configuration
# Change YOUR_PG_PASSWORD to your PostgreSQL password (set during install)
DATABASE_URL="postgresql://postgres:YOUR_PG_PASSWORD@localhost:5432/chatcrm"
JWT_SECRET="chatcrm-secret-change-in-production-abc123xyz"
PORT=5000
NODE_ENV=development
